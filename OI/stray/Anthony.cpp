#include "Anthony.h"
#include <vector>

namespace {

int FunctionExample(int i, int A) {
  return i % A;
}

}  // namespace

std::vector<int> Mark(int N, int M, int A, int B,
                      std::vector<int> U, std::vector<int> V) {
  const int col[6] = {0, 1, 0, 0, 1, 1};
  std::vector<int> X(M);
  vector<vector<int>> G(N);
  return X;
}

// 110010
