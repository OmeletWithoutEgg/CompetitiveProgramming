#include <bits/stdc++.h>

using namespace std;

int readInt(int l, int r) {
    int x;
    assert (cin >> x && l <= x && x <= r);
    return x;
}
signed main() {
    int n = readInt(1, 1000);
    int m = readInt(1, 1000000000);
    for (int i = 0; i < n-1; i++) {
        int t = readInt(0, 1000000000);
    }
    for (int i = 0; i < n; i++) {
        int s = readInt(1, 1000000000);
    }
    for (int i = 0; i < n; i++) {
        int d = readInt(0, 1000000000);
    }
}
