#include<bits/stdc++.h>
#include "grader.h"

using namespace std;

vector<int> findSequence (int n) {
    int h = n / 2;
    int d = isSubsequence(vector<int>(h+1, 1));
    for(int i = 0; i < h; i++) {
        ;
    }
}

