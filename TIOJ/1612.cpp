#include <bits/stdc++.h>
#define int long long
#define ff first
#define ss second
#define N 3000

using namespace std;
int n,k,X,d;
int dp[N+1][N+1] = {};
pair<int,int> a[N+1] = {};
signed main(){
    cin >> n >> k >> X >> d;
    REP(i,0,n)cin >> a[i].ff >> a[i].ss;
    sort(a,a+n);

    cout << dp[]
}
