#include <bits/stdc++.h>
#define debug(x) cout << #x << ':' << x << '\n'
#define MAX_C

using namespace std;
typedef long long ll;

struct dat{
    vector<size_t> v;
    dat(const string&s):v(MAX_C,0){
        for(auto c:s)
    }
};
signed main(){
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n;
    string s;
    map<dat,int> st;
    while(cin >> n,n){
        while(n--){
            cin >> s;

        }
    }
}

